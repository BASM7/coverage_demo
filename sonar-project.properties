sonar.projectKey=BASM7_coverage_demo
sonar.organization=kaizen4ever

# This is the name and version displayed in the SonarCloud UI.
sonar.projectName=coverage_demo
sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=.

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8
sonar.python.version=3.10
sonar.python.coverage.reportPaths=coverage.xml

sonar.coverage.exclusions=*tests*,login/migrations/*,*manage.py,*__init__.py,**/migrations/**,*asgi*,*wsgi*,*admin.py,*urls.py